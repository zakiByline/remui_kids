{{!
    Elementary Profile Page - Professional UI
    Shows real user profile data from Moodle
}}

{{! Include Sidebar }}
{{> theme_remui_kids/dashboard/elementary_sidebar }}

<style>
/* Force body scroll lock */
html, body {
    overflow: hidden !important;
    height: 100vh !important;
    margin: 0 !important;
    padding: 0 !important;
}

/* Hide default containers */
.page-header,
.context-header {
    display: none !important;
}

.container,
.container-fluid {
    margin: 0 !important;
    padding: 0 !important;
    max-width: 100% !important;
    width: 100% !important;
}

#page {
    margin: 0 !important;
    padding: 0 !important;
    background: transparent !important;
}

/* Main Container */
.profile-main-container {
    position: fixed;
    top: 0;
    left: 280px;
    right: 0;
    bottom: 0;
    padding: 100px 30px 30px 30px;
    background: #f5f7fa;
    overflow-y: auto;
    overflow-x: hidden;
}

/* Welcome Header with Bubble Animations */
.welcome-header {
    background: #ffffff;
    border-radius: 24px;
    padding: 50px 40px;
    margin-bottom: 30px;
    box-shadow: 0 4px 20px rgba(129, 140, 248, 0.15);
    text-align: center;
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    min-height: 180px;
    border: 1px solid rgba(129, 140, 248, 0.1);
}

.welcome-header-content {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    flex: 1;
}

/* Animated Bubbles */
.welcome-bubbles {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 200px;
    pointer-events: none;
    z-index: 1;
}

.welcome-bubbles-left {
    left: -50px;
}

.welcome-bubbles-right {
    right: -50px;
}

.welcome-bubble {
    position: absolute;
    border-radius: 50%;
    background: rgba(129, 140, 248, 0.15);
    backdrop-filter: blur(10px);
    animation: floatBubble 15s ease-in-out infinite;
}

.welcome-bubble-1 {
    width: 80px;
    height: 80px;
    top: 10%;
    left: 20%;
    animation-delay: 0s;
}

.welcome-bubble-2 {
    width: 60px;
    height: 60px;
    top: 50%;
    left: 10%;
    animation-delay: 1.5s;
}

.welcome-bubble-3 {
    width: 100px;
    height: 100px;
    top: 70%;
    left: 30%;
    animation-delay: 3s;
}

.welcome-bubble-4 {
    width: 50px;
    height: 50px;
    top: 30%;
    left: 50%;
    animation-delay: 0.5s;
}

.welcome-bubble-5 {
    width: 90px;
    height: 90px;
    top: 15%;
    right: 15%;
    animation-delay: 2s;
}

.welcome-bubble-6 {
    width: 70px;
    height: 70px;
    top: 45%;
    right: 25%;
    animation-delay: 0.8s;
}

.welcome-bubble-7 {
    width: 55px;
    height: 55px;
    top: 65%;
    right: 10%;
    animation-delay: 2.5s;
}

.welcome-bubble-8 {
    width: 85px;
    height: 85px;
    top: 25%;
    right: 40%;
    animation-delay: 1.2s;
}

@keyframes floatBubble {
    0%, 100% {
        transform: translateY(0px) translateX(0px) scale(1);
        opacity: 0.6;
    }
    25% {
        transform: translateY(-8px) translateX(4px) scale(1.03);
        opacity: 0.7;
    }
    50% {
        transform: translateY(-4px) translateX(-2px) scale(0.98);
        opacity: 0.65;
    }
    75% {
        transform: translateY(-10px) translateX(6px) scale(1.02);
        opacity: 0.72;
    }
}

/* Student Profile Picture in Welcome Header */
.welcome-student-picture {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    border: 4px solid rgba(129, 140, 248, 0.2);
    object-fit: cover;
    box-shadow: 0 4px 15px rgba(129, 140, 248, 0.2);
    background: white;
}

.welcome-title {
    font-size: 2.5rem;
    color: #6366f1;
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
    position: relative;
    z-index: 1;
    text-shadow: none;
    font-weight: 700;
}

.welcome-title i {
    color: #818cf8;
    filter: none;
}

.welcome-subtitle {
    color: #6b7280;
    font-size: 1.2rem;
    position: relative;
    z-index: 1;
    text-shadow: none;
}

.profile-header-left h1 {
    font-size: 2rem;
    color: #2c3e50;
    margin: 0 0 5px 0;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 15px;
}

.profile-header-left h1 i {
    color: #4A90E2;
}

.profile-header-left p {
    color: #7f8c8d;
    margin: 0;
    font-size: 1rem;
}

/* Profile Content Grid */
.profile-content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 25px;
    margin-bottom: 25px;
}

/* Profile Card */
.profile-card {
    background: white;
    border-radius: 12px;
    padding: 30px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    border: 2px solid transparent;
    transition: all 0.3s ease;
}

.profile-card:hover {
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.12);
    transform: translateY(-2px);
}

.profile-card-header {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-bottom: 25px;
    padding-bottom: 20px;
    border-bottom: 2px solid #f8f9fa;
}

.profile-card-icon {
    width: 50px;
    height: 50px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    color: white;
}

.profile-card-icon.personal {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.profile-card-icon.academic {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

.profile-card-icon.achievements {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}

.profile-card-icon.activity {
    background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
}

.profile-card-title {
    font-size: 1.3rem;
    font-weight: 700;
    color: #2c3e50;
    margin: 0;
}

/* Profile Picture Section */
.profile-picture-section {
    text-align: center;
    margin-bottom: 30px;
}

.profile-picture {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    border: 4px solid #4A90E2;
    margin: 0 auto 20px auto;
    display: block;
    object-fit: cover;
    box-shadow: 0 4px 15px rgba(74, 144, 226, 0.3);
}

.profile-name {
    font-size: 1.8rem;
    font-weight: 700;
    color: #2c3e50;
    margin: 0 0 10px 0;
}

.profile-grade {
    font-size: 1.1rem;
    color: #4A90E2;
    font-weight: 600;
    margin: 0 0 20px 0;
}

/* Profile Info Grid */
.profile-info-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
    margin-bottom: 20px;
}

.profile-info-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px;
    background: #f8f9fa;
    border-radius: 8px;
    border-left: 3px solid #4A90E2;
}

.profile-info-item i {
    color: #4A90E2;
    font-size: 1.1rem;
    width: 20px;
    text-align: center;
}

.profile-info-label {
    font-size: 0.9rem;
    color: #7f8c8d;
    font-weight: 500;
    margin: 0;
}

.profile-info-value {
    font-size: 0.9rem;
    color: #2c3e50;
    font-weight: 600;
    margin: 0;
}

/* Statistics Grid */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 15px;
    margin-bottom: 20px;
}

.stat-item {
    text-align: center;
    padding: 20px 15px;
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    border-radius: 10px;
    border: 2px solid transparent;
    transition: all 0.3s ease;
}

.stat-item:hover {
    border-color: #4A90E2;
    transform: translateY(-2px);
}

.stat-number {
    font-size: 2rem;
    font-weight: 700;
    color: #4A90E2;
    margin: 0 0 5px 0;
}

.stat-label {
    font-size: 0.85rem;
    color: #7f8c8d;
    font-weight: 600;
    margin: 0;
}

/* Progress Bars */
.progress-section {
    margin-top: 20px;
}

.progress-item {
    margin-bottom: 15px;
}

.progress-label {
    display: flex;
    justify-content: space-between;
    margin-bottom: 8px;
    font-size: 0.9rem;
    color: #5a6c7d;
    font-weight: 600;
}

.progress-bar-wrapper {
    height: 8px;
    background: #e8ecef;
    border-radius: 4px;
    overflow: hidden;
}

.progress-bar-inner {
    height: 100%;
    border-radius: 4px;
    transition: width 0.3s ease;
}

.progress-bar-inner.courses {
    background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
}

.progress-bar-inner.activities {
    background: linear-gradient(90deg, #f093fb 0%, #f5576c 100%);
}

.progress-bar-inner.badges {
    background: linear-gradient(90deg, #4facfe 0%, #00f2fe 100%);
}

/* Cohort List */
.cohort-list {
    list-style: none;
    padding: 0;
    margin: 0;
}

.cohort-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 15px;
    background: #f8f9fa;
    border-radius: 8px;
    margin-bottom: 8px;
    border-left: 3px solid #4A90E2;
}

.cohort-item i {
    color: #4A90E2;
    font-size: 1rem;
}

.cohort-name {
    font-size: 0.9rem;
    color: #2c3e50;
    font-weight: 600;
    margin: 0;
}

/* Responsive Design */
@media (max-width: 1200px) {
    .profile-content-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .profile-main-container {
        left: 0;
        padding: 20px 15px;
    }
    
    .welcome-title {
        font-size: 2rem;
    }
    
    .welcome-student-picture {
        width: 60px;
        height: 60px;
        border-width: 3px;
    }
    
    .welcome-header {
        padding: 30px 20px;
        min-height: 150px;
    }
    
    .profile-card {
        padding: 20px;
    }
    
    .profile-info-grid {
        grid-template-columns: 1fr;
    }
    
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
    }
    
    .profile-picture {
        width: 100px;
        height: 100px;
    }
    
    .profile-name {
        font-size: 1.5rem;
    }
}

@media (max-width: 480px) {
    .stats-grid {
        grid-template-columns: 1fr;
    }
    
    .welcome-title {
        font-size: 1.75rem;
    }
}
</style>

<div class="profile-main-container">
    <!-- Welcome Header with Bubble Animations -->
    <div class="welcome-header">
        <!-- Left Side Bubbles -->
        <div class="welcome-bubbles welcome-bubbles-left">
            <div class="welcome-bubble welcome-bubble-1"></div>
            <div class="welcome-bubble welcome-bubble-2"></div>
            <div class="welcome-bubble welcome-bubble-3"></div>
            <div class="welcome-bubble welcome-bubble-4"></div>
        </div>
        
        <!-- Center Content -->
        <div class="welcome-header-content">
            {{#student_picture_url_only}}
            <img src="{{student_picture_url_only}}" alt="{{student_name}}" class="welcome-student-picture">
            {{/student_picture_url_only}}
            <h1 class="welcome-title">
                <i class="fa fa-user"></i>
                My Profile
            </h1>
            <p class="welcome-subtitle">
                View and manage your personal information and learning progress
            </p>
        </div>
        
        <!-- Right Side Bubbles -->
        <div class="welcome-bubbles welcome-bubbles-right">
            <div class="welcome-bubble welcome-bubble-5"></div>
            <div class="welcome-bubble welcome-bubble-6"></div>
            <div class="welcome-bubble welcome-bubble-7"></div>
            <div class="welcome-bubble welcome-bubble-8"></div>
        </div>
    </div>

    <!-- Profile Content Grid -->
    <div class="profile-content-grid">
        <!-- Personal Information Card -->
        <div class="profile-card">
            <div class="profile-card-header">
                <div class="profile-card-icon personal">
                    <i class="fa fa-user-circle"></i>
                </div>
                <h3 class="profile-card-title">Personal Information</h3>
            </div>
            
            <!-- Profile Picture -->
            <div class="profile-picture-section">
                {{#user_data.has_profile_picture}}
                <img src="{{user_data.profile_picture}}" alt="Profile Picture" class="profile-picture" onerror="this.style.display='none'; this.parentElement.querySelector('.profile-picture-placeholder').style.display='flex';">
                {{/user_data.has_profile_picture}}
                <div class="profile-picture-placeholder" style="{{#user_data.has_profile_picture}}display: none;{{/user_data.has_profile_picture}}{{^user_data.has_profile_picture}}display: flex;{{/user_data.has_profile_picture}} width: 120px; height: 120px; border-radius: 50%; border: 4px solid #4A90E2; margin: 0 auto 20px auto; align-items: center; justify-content: center; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; font-size: 2.5rem; font-weight: bold;">
                    {{user_data.initials}}
                </div>
                <h2 class="profile-name">{{user_data.fullname}}</h2>
                <p class="profile-grade">{{user_data.grade_level}} Student</p>
            </div>
            
            <!-- Profile Info -->
            <div class="profile-info-grid">
                <div class="profile-info-item">
                    <i class="fa fa-user"></i>
                    <div>
                        <p class="profile-info-label">Username</p>
                        <p class="profile-info-value">{{user_data.username}}</p>
                    </div>
                </div>
                
                <div class="profile-info-item">
                    <i class="fa fa-envelope"></i>
                    <div>
                        <p class="profile-info-label">Email</p>
                        <p class="profile-info-value">{{user_data.email}}</p>
                    </div>
                </div>
                
                {{#user_data.city}}
                <div class="profile-info-item">
                    <i class="fa fa-map-marker"></i>
                    <div>
                        <p class="profile-info-label">City</p>
                        <p class="profile-info-value">{{user_data.city}}</p>
                    </div>
                </div>
                {{/user_data.city}}
                
                {{#user_data.country}}
                <div class="profile-info-item">
                    <i class="fa fa-globe"></i>
                    <div>
                        <p class="profile-info-label">Country</p>
                        <p class="profile-info-value">{{user_data.country}}</p>
                    </div>
                </div>
                {{/user_data.country}}
            </div>
        </div>

        <!-- Academic Progress Card -->
        <div class="profile-card">
            <div class="profile-card-header">
                <div class="profile-card-icon academic">
                    <i class="fa fa-graduation-cap"></i>
                </div>
                <h3 class="profile-card-title">Academic Progress</h3>
            </div>
            
            <!-- Statistics -->
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-number">{{user_data.total_courses}}</div>
                    <div class="stat-label">Total Courses</div>
                </div>
                
                <div class="stat-item">
                    <div class="stat-number">{{user_data.completed_courses}}</div>
                    <div class="stat-label">Completed</div>
                </div>
                
                <div class="stat-item">
                    <div class="stat-number">{{user_data.in_progress_courses}}</div>
                    <div class="stat-label">In Progress</div>
                </div>
            </div>
            
            <!-- Progress Bars -->
            <div class="progress-section">
                <div class="progress-item">
                    <div class="progress-label">
                        <span>Course Completion</span>
                        <span>{{user_data.course_progress}}%</span>
                    </div>
                    <div class="progress-bar-wrapper">
                        <div class="progress-bar-inner courses" style="width: {{user_data.course_progress}}%"></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Activity Statistics Card -->
        <div class="profile-card">
            <div class="profile-card-header">
                <div class="profile-card-icon activity">
                    <i class="fa fa-tasks"></i>
                </div>
                <h3 class="profile-card-title">Activity Statistics</h3>
            </div>
            
            <!-- Statistics -->
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-number">{{user_data.total_activities}}</div>
                    <div class="stat-label">Total Activities</div>
                </div>
                
                <div class="stat-item">
                    <div class="stat-number">{{user_data.completed_activities}}</div>
                    <div class="stat-label">Completed</div>
                </div>
            </div>
            
            <!-- Progress Bar -->
            <div class="progress-section">
                <div class="progress-item">
                    <div class="progress-label">
                        <span>Activity Completion</span>
                        <span>{{user_data.activity_progress}}%</span>
                    </div>
                    <div class="progress-bar-wrapper">
                        <div class="progress-bar-inner activities" style="width: {{user_data.activity_progress}}%"></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Achievements Card -->
        <div class="profile-card">
            <div class="profile-card-header">
                <div class="profile-card-icon achievements">
                    <i class="fa fa-trophy"></i>
                </div>
                <h3 class="profile-card-title">Achievements</h3>
            </div>
            
            <!-- Statistics -->
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-number">{{user_data.total_badges}}</div>
                    <div class="stat-label">Available Badges</div>
                </div>
                
                <div class="stat-item">
                    <div class="stat-number">{{user_data.earned_badges}}</div>
                    <div class="stat-label">Earned Badges</div>
                </div>
            </div>
            
            <!-- Progress Bar -->
            <div class="progress-section">
                <div class="progress-item">
                    <div class="progress-label">
                        <span>Badge Progress</span>
                        <span>{{user_data.badge_progress}}%</span>
                    </div>
                    <div class="progress-bar-wrapper">
                        <div class="progress-bar-inner badges" style="width: {{user_data.badge_progress}}%"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Additional Information Card -->
    <div class="profile-card">
        <div class="profile-card-header">
            <div class="profile-card-icon personal">
                <i class="fa fa-info-circle"></i>
            </div>
            <h3 class="profile-card-title">Additional Information</h3>
        </div>
        
        <div class="profile-info-grid">
            <div class="profile-info-item">
                <i class="fa fa-calendar"></i>
                <div>
                    <p class="profile-info-label">First Access</p>
                    <p class="profile-info-value">{{user_data.firstaccess_formatted}}</p>
                </div>
            </div>
            
            <div class="profile-info-item">
                <i class="fa fa-clock-o"></i>
                <div>
                    <p class="profile-info-label">Last Access</p>
                    <p class="profile-info-value">{{user_data.lastaccess_formatted}}</p>
                </div>
            </div>
            
            <div class="profile-info-item">
                <i class="fa fa-sign-in"></i>
                <div>
                    <p class="profile-info-label">Last Login</p>
                    <p class="profile-info-value">{{user_data.lastlogin_formatted}}</p>
                </div>
            </div>
            
            <div class="profile-info-item">
                <i class="fa fa-language"></i>
                <div>
                    <p class="profile-info-label">Language</p>
                    <p class="profile-info-value">{{user_data.lang}}</p>
                </div>
            </div>
        </div>
        
        <!-- Cohort Information -->
        {{#user_data.cohorts}}
        {{#.}}
        <div style="margin-top: 20px;">
            <h4 style="color: #2c3e50; margin-bottom: 15px; font-size: 1.1rem;">
                <i class="fa fa-users" style="color: #4A90E2; margin-right: 8px;"></i>
                Groups & Classes
            </h4>
            <ul class="cohort-list">
                {{#user_data.cohorts}}
                <li class="cohort-item">
                    <i class="fa fa-graduation-cap"></i>
                    <p class="cohort-name">{{name}}</p>
                </li>
                {{/user_data.cohorts}}
            </ul>
        </div>
        {{/.}}
        {{/user_data.cohorts}}
    </div>
</div>


